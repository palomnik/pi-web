# Pi Web Interface - Environment Variables
# Copy this file to .env and customize values

# Server configuration
PI_WEB_PORT=3300
PI_WEB_HOST=localhost

# Authentication (REQUIRED — auth is enabled by default)
# You MUST set both username and password to be able to log in.
# Without these, login will be impossible.
PI_WEB_USERNAME=
PI_WEB_PASSWORD=